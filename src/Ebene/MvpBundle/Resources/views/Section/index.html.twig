{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Section list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Couleur1</th>
                <th>Couleur2</th>
                <th>Couleur3</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('mvp_section_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.couleur1 }}</td>
                <td>{{ entity.couleur2 }}</td>
                <td>{{ entity.couleur3 }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('mvp_section_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mvp_section_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('mvp_section_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
